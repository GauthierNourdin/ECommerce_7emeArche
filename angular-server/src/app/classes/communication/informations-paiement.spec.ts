import { InformationsPaiement } from './informations-paiement';

describe('InformationsPaiement', () => {
  it('should create an instance', () => {
    expect(new InformationsPaiement()).toBeTruthy();
  });
});

import { CommandeAvecIdClient } from './commande-avec-id-client';

describe('CommandeAvecIdClient', () => {
  it('should create an instance', () => {
    expect(new CommandeAvecIdClient()).toBeTruthy();
  });
});

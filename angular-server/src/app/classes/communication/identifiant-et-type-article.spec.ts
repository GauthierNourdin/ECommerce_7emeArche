import { IdentifiantEtTypeArticle } from './identifiant-et-type-article';

describe('IdentifiantEtTypeArticle', () => {
  it('should create an instance', () => {
    expect(new IdentifiantEtTypeArticle()).toBeTruthy();
  });
});

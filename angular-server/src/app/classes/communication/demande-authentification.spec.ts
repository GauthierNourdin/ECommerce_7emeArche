import { DemandeAuthentification } from './demande-authentification';

describe('DemandeAuthentification', () => {
  it('should create an instance', () => {
    expect(new DemandeAuthentification()).toBeTruthy();
  });
});

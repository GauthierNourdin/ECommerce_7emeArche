import { ConsultationAvecIdClient } from './consultation-avec-id-client';

describe('ConsultationAvecIdClient', () => {
  it('should create an instance', () => {
    expect(new ConsultationAvecIdClient()).toBeTruthy();
  });
});
